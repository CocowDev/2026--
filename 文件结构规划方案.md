# 📁 星级酒店预订与管理系统 — 文件结构规划方案

## 一、项目根目录结构

```
hotel-booking-system/           # 项目根目录
├── frontend/                   # 前端项目（Vue 3）
│   └── src/                    # 前端源代码
├── backend/                    # 后端项目（Node.js + Express）
│   └── src/                    # 后端源代码
├── database/                   # 数据库相关文件
├── README.md                   # 项目说明文档【新建】
└── package.json                # 根目录依赖管理【新建】
```

---

## 二、后端目录结构（Node.js + Express）

```
backend/
├── src/                        # 后端源代码
│   ├── app.js                  # Express 应用入口【新建】
│   ├── server.js               # 服务器启动文件【新建】
│   ├── config/                 # 配置文件目录【新建】
│   │   ├── database.js         # 数据库连接配置【新建】
│   │   └── jwt.js              # JWT 配置【新建】
│   ├── middleware/             # 中间件目录【新建】
│   │   ├── auth.js             # 身份认证中间件【新建】
│   │   ├── validation.js       # 请求参数校验中间件【新建】
│   │   └── errorHandler.js     # 错误处理中间件【新建】
│   ├── models/                 # 数据模型目录【新建】
│   │   ├── User.js             # 用户模型【新建】
│   │   ├── Booking.js          # 预订模型【新建】
│   │   ├── RoomType.js         # 房型模型【新建】
│   │   ├── Restaurant.js       # 餐厅模型【新建】
│   │   └── Admin.js            # 管理员模型【新建】
│   ├── routes/                 # 路由目录【新建】
│   │   ├── auth.js             # 认证路由（登录、注册）【新建】
│   │   ├── bookings.js         # 预订路由【新建】
│   │   ├── users.js            # 用户路由【新建】
│   │   └── dashboard.js        # 仪表盘统计路由【新建】
│   ├── controllers/            # 控制器目录【新建】
│   │   ├── authController.js   # 认证控制器【新建】
│   │   ├── bookingController.js # 预订控制器【新建】
│   │   ├── userController.js   # 用户控制器【新建】
│   │   └── dashboardController.js # 仪表盘控制器【新建】
│   ├── services/               # 服务层目录【新建】
│   │   ├── authService.js      # 认证服务【新建】
│   │   ├── bookingService.js   # 预订服务【新建】
│   │   └── userService.js      # 用户服务【新建】
│   └── utils/                  # 工具函数目录【新建】
│       ├── response.js         # 统一响应格式工具【新建】
│       ├── password.js         # 密码加密工具【新建】
│       └── jwt.js              # JWT 工具【新建】
├── .env                        # 环境变量配置文件【新建】
├── package.json                # 后端依赖管理【新建】
├── package-lock.json           # 依赖锁定文件【新建】
├── .gitignore                  # Git 忽略配置【新建】
└── README.md                   # 后端说明文档【新建】
```

### 后端文件详细说明

| 文件路径 | 类型 | 标签 | 功能说明 |
|---------|------|------|---------|
| `src/app.js` | JS | 新建 | Express 应用主文件，配置中间件和路由 |
| `src/server.js` | JS | 新建 | 服务器启动入口，监听端口 |
| `src/config/database.js` | JS | 新建 | Sequelize 数据库连接配置 |
| `src/config/jwt.js` | JS | 新建 | JWT 密钥和过期时间配置 |
| `src/middleware/auth.js` | JS | 新建 | JWT 验证中间件，保护需要登录的接口 |
| `src/middleware/validation.js` | JS | 新建 | 请求参数校验中间件 |
| `src/middleware/errorHandler.js` | JS | 新建 | 全局错误处理中间件 |
| `src/models/User.js` | JS | 新建 | 用户数据模型，定义用户表结构 |
| `src/models/Booking.js` | JS | 新建 | 预订数据模型，定义预订表结构 |
| `src/models/RoomType.js` | JS | 新建 | 房型数据模型，定义房型表结构 |
| `src/models/Restaurant.js` | JS | 新建 | 餐厅数据模型，定义餐厅表结构 |
| `src/models/Admin.js` | JS | 新建 | 管理员数据模型，定义管理员表结构 |
| `src/routes/auth.js` | JS | 新建 | 认证相关路由（登录、注册） |
| `src/routes/bookings.js` | JS | 新建 | 预订管理路由（增删改查） |
| `src/routes/users.js` | JS | 新建 | 用户管理路由 |
| `src/routes/dashboard.js` | JS | 新建 | 仪表盘统计数据路由 |
| `src/controllers/authController.js` | JS | 新建 | 认证控制器，处理登录注册逻辑 |
| `src/controllers/bookingController.js` | JS | 新建 | 预订控制器，处理预订业务逻辑 |
| `src/controllers/userController.js` | JS | 新建 | 用户控制器，处理用户管理逻辑 |
| `src/controllers/dashboardController.js` | JS | 新建 | 仪表盘控制器，处理统计数据逻辑 |
| `src/services/authService.js` | JS | 新建 | 认证服务，封装认证业务逻辑 |
| `src/services/bookingService.js` | JS | 新建 | 预订服务，封装预订业务逻辑 |
| `src/services/userService.js` | JS | 新建 | 用户服务，封装用户业务逻辑 |
| `src/utils/response.js` | JS | 新建 | 统一响应格式封装 |
| `src/utils/password.js` | JS | 新建 | bcrypt 密码加密工具 |
| `src/utils/jwt.js` | JS | 新建 | JWT 生成和验证工具 |
| `.env` | 配置 | 新建 | 环境变量（数据库连接、JWT密钥等） |
| `package.json` | JSON | 新建 | 后端依赖配置 |

---

## 三、前端目录结构（Vue 3 + TypeScript）

```
frontend/
├── src/                        # 前端源代码
│   ├── main.ts                 # 应用入口【迁移】
│   ├── App.vue                 # 根组件【迁移】
│   ├── router/                 # 路由目录【迁移】
│   │   └── index.ts            # 路由配置【迁移】
│   ├── stores/                 # Pinia 状态管理目录【新建】
│   │   ├── user.ts             # 用户状态管理【新建】
│   │   ├── booking.ts          # 预订状态管理【新建】
│   │   └── admin.ts            # 管理员状态管理【新建】
│   ├── api/                    # API 接口层目录【新建】
│   │   ├── request.ts          # Axios 封装【新建】
│   │   ├── auth.ts             # 认证接口【新建】
│   │   ├── bookings.ts         # 预订接口【新建】
│   │   ├── users.ts            # 用户接口【新建】
│   │   └── dashboard.ts        # 仪表盘接口【新建】
│   ├── types/                  # TypeScript 类型定义目录【新建】
│   │   └── index.ts            # 全局类型定义【新建】
│   ├── styles/                 # 样式目录【新建】
│   │   ├── variables.scss      # SCSS 变量【新建】
│   │   ├── mixins.scss         # SCSS 混合宏【新建】
│   │   └── global.scss         # 全局样式【新建】
│   ├── components/             # 通用组件目录【迁移】
│   │   ├── NavBar.vue          # 导航栏【迁移】
│   │   ├── Footer.vue          # 页脚【迁移】
│   │   ├── Carousel.vue        # 轮播图【迁移】
│   │   ├── RoomList.vue        # 客房列表【迁移】
│   │   ├── DiningList.vue      # 餐饮列表【迁移】
│   │   └── ServiceList.vue     # 服务列表【迁移】
│   ├── pages/                  # 前台页面目录【迁移】
│   │   ├── Home.vue            # 首页【迁移】
│   │   ├── RoomBooking.vue     # 预订页面【迁移】
│   │   ├── Login.vue           # 登录页面【迁移】
│   │   ├── Register.vue        # 注册页面【迁移】
│   │   ├── News.vue            # 新闻页面【迁移】
│   │   ├── About.vue           # 关于页面【迁移】
│   │   ├── Contact.vue         # 联系页面【迁移】
│   │   ├── RoomEnvironment.vue # 客房环境页面【迁移】
│   │   └── DiningBooking.vue   # 餐饮预订页面【迁移】
│   ├── pages/admin/            # 后台管理页面目录【迁移】
│   │   ├── AdminLayout.vue     # 后台布局框架【迁移】
│   │   ├── AdminLogin.vue      # 管理员登录【迁移】
│   │   ├── AdminDashboard.vue  # 数据概览【迁移】
│   │   ├── BookingsList.vue    # 预订列表管理【迁移】
│   │   ├── BookingDetail.vue   # 预订详情【迁移】
│   │   ├── UsersManage.vue     # 用户管理【迁移】
│   │   └── AdminSettings.vue   # 系统设置【迁移】
│   └── utils/                  # 工具函数目录【迁移】
│       ├── date.ts             # 日期工具函数【迁移】
│       └── validation.ts       # 表单验证工具函数【迁移】
├── public/                     # 静态资源目录【迁移】
├── index.html                  # HTML 入口文件【迁移】
├── package.json                # 前端依赖管理【迁移】
├── tsconfig.json               # TypeScript 配置【新建】
├── vite.config.ts              # Vite 配置【新建】
├── .gitignore                  # Git 忽略配置【迁移】
└── README.md                   # 前端说明文档【迁移】
```

### 前端文件详细说明

| 文件路径 | 类型 | 标签 | 功能说明 |
|---------|------|------|---------|
| `src/main.ts` | TS | 迁移 | 应用入口，注册路由、Pinia、Element Plus |
| `src/App.vue` | Vue | 迁移 | 根组件，页面骨架和全局样式 |
| `src/router/index.ts` | TS | 迁移 | 路由配置（前台 + 后台嵌套路由） |
| `src/stores/user.ts` | TS | 新建 | 用户状态管理（登录、注册、用户信息） |
| `src/stores/booking.ts` | TS | 新建 | 预订状态管理（预订列表、创建预订） |
| `src/stores/admin.ts` | TS | 新建 | 管理员状态管理（管理员登录、权限） |
| `src/api/request.ts` | TS | 新建 | Axios 封装，配置请求/响应拦截器 |
| `src/api/auth.ts` | TS | 新建 | 认证 API 接口封装 |
| `src/api/bookings.ts` | TS | 新建 | 预订 API 接口封装 |
| `src/api/users.ts` | TS | 新建 | 用户 API 接口封装 |
| `src/api/dashboard.ts` | TS | 新建 | 仪表盘 API 接口封装 |
| `src/types/index.ts` | TS | 新建 | TypeScript 类型定义（User、Booking 等） |
| `src/styles/variables.scss` | SCSS | 新建 | SCSS 变量（颜色、字体、间距等） |
| `src/styles/mixins.scss` | SCSS | 新建 | SCSS 混合宏（常用样式片段） |
| `src/styles/global.scss` | SCSS | 新建 | 全局样式（重置样式、通用类） |
| `src/components/NavBar.vue` | Vue | 迁移 | 导航栏组件（响应式、搜索功能） |
| `src/components/Footer.vue` | Vue | 迁移 | 页脚组件（快速链接、社交图标） |
| `src/components/Carousel.vue` | Vue | 迁移 | 轮播图组件（自动播放、手动切换） |
| `src/components/RoomList.vue` | Vue | 迁移 | 客房列表卡片组件 |
| `src/components/DiningList.vue` | Vue | 迁移 | 餐饮列表卡片组件 |
| `src/components/ServiceList.vue` | Vue | 迁移 | 服务列表卡片组件 |
| `src/pages/Home.vue` | Vue | 迁移 | 首页页面 |
| `src/pages/RoomBooking.vue` | Vue | 迁移 | 预订页面（客房、餐饮、服务） |
| `src/pages/Login.vue` | Vue | 迁移 | 用户登录页面 |
| `src/pages/Register.vue` | Vue | 迁移 | 用户注册页面 |
| `src/pages/News.vue` | Vue | 迁移 | 新闻页面 |
| `src/pages/About.vue` | Vue | 迁移 | 关于页面 |
| `src/pages/Contact.vue` | Vue | 迁移 | 联系页面 |
| `src/pages/RoomEnvironment.vue` | Vue | 迁移 | 客房环境页面 |
| `src/pages/DiningBooking.vue` | Vue | 迁移 | 餐饮预订页面 |
| `src/pages/admin/AdminLayout.vue` | Vue | 迁移 | 后台布局框架 |
| `src/pages/admin/AdminLogin.vue` | Vue | 迁移 | 管理员登录页面 |
| `src/pages/admin/AdminDashboard.vue` | Vue | 迁移 | 数据概览页面 |
| `src/pages/admin/BookingsList.vue` | Vue | 迁移 | 预订列表管理页面 |
| `src/pages/admin/BookingDetail.vue` | Vue | 迁移 | 预订详情页面 |
| `src/pages/admin/UsersManage.vue` | Vue | 迁移 | 用户管理页面 |
| `src/pages/admin/AdminSettings.vue` | Vue | 迁移 | 系统设置页面 |
| `src/utils/date.ts` | TS | 迁移 | 日期工具函数（格式化、计算等） |
| `src/utils/validation.ts` | TS | 迁移 | 表单验证工具函数 |
| `tsconfig.json` | JSON | 新建 | TypeScript 编译配置 |
| `vite.config.ts` | TS | 新建 | Vite 构建配置（含代理配置） |

---

## 四、数据库目录结构

```
database/
├── init.sql                    # 数据库初始化脚本【新建】
├── seed.sql                    # 种子数据脚本【新建】
└── schema.sql                  # 表结构定义【新建】
```

### 数据库文件详细说明

| 文件路径 | 类型 | 标签 | 功能说明 |
|---------|------|------|---------|
| `init.sql` | SQL | 新建 | 创建数据库和初始表结构 |
| `seed.sql` | SQL | 新建 | 插入示例数据（用户、房型、预订等） |
| `schema.sql` | SQL | 新建 | 数据库表结构定义文档 |

---

## 五、新增文件与迁移文件统计

### 新增文件（54个）

| 类别 | 数量 | 说明 |
|------|------|------|
| 后端配置文件 | 2 | .env, package.json |
| 后端入口文件 | 2 | app.js, server.js |
| 后端配置目录 | 2 | database.js, jwt.js |
| 后端中间件 | 3 | auth.js, validation.js, errorHandler.js |
| 后端模型 | 5 | User.js, Booking.js, RoomType.js, Restaurant.js, Admin.js |
| 后端路由 | 4 | auth.js, bookings.js, users.js, dashboard.js |
| 后端控制器 | 4 | authController.js, bookingController.js, userController.js, dashboardController.js |
| 后端服务 | 3 | authService.js, bookingService.js, userService.js |
| 后端工具 | 3 | response.js, password.js, jwt.js |
| 前端状态管理 | 3 | user.ts, booking.ts, admin.ts |
| 前端 API 层 | 5 | request.ts, auth.ts, bookings.ts, users.ts, dashboard.ts |
| 前端类型定义 | 1 | index.ts |
| 前端样式 | 3 | variables.scss, mixins.scss, global.scss |
| 前端配置文件 | 2 | tsconfig.json, vite.config.ts |
| 数据库文件 | 3 | init.sql, seed.sql, schema.sql |

### 迁移文件（26个）

| 类别 | 数量 | 说明 |
|------|------|------|
| 前端入口文件 | 2 | main.ts, App.vue |
| 前端路由 | 1 | router/index.ts |
| 前端组件 | 6 | NavBar.vue, Footer.vue, Carousel.vue, RoomList.vue, DiningList.vue, ServiceList.vue |
| 前台页面 | 9 | Home.vue, RoomBooking.vue, Login.vue, Register.vue, News.vue, About.vue, Contact.vue, RoomEnvironment.vue, DiningBooking.vue |
| 后台页面 | 7 | AdminLayout.vue, AdminLogin.vue, AdminDashboard.vue, BookingsList.vue, BookingDetail.vue, UsersManage.vue, AdminSettings.vue |
| 前端工具 | 2 | date.ts, validation.ts |

---

## 六、目录层级结构图

```
hotel-booking-system/
├── frontend/
│   ├── src/
│   │   ├── api/
│   │   │   ├── auth.ts
│   │   │   ├── bookings.ts
│   │   │   ├── dashboard.ts
│   │   │   ├── request.ts
│   │   │   └── users.ts
│   │   ├── components/
│   │   │   ├── Carousel.vue
│   │   │   ├── DiningList.vue
│   │   │   ├── Footer.vue
│   │   │   ├── NavBar.vue
│   │   │   ├── RoomList.vue
│   │   │   └── ServiceList.vue
│   │   ├── pages/
│   │   │   ├── admin/
│   │   │   │   ├── AdminDashboard.vue
│   │   │   │   ├── AdminLayout.vue
│   │   │   │   ├── AdminLogin.vue
│   │   │   │   ├── AdminSettings.vue
│   │   │   │   ├── BookingDetail.vue
│   │   │   │   ├── BookingsList.vue
│   │   │   │   └── UsersManage.vue
│   │   │   ├── About.vue
│   │   │   ├── Contact.vue
│   │   │   ├── DiningBooking.vue
│   │   │   ├── Home.vue
│   │   │   ├── Login.vue
│   │   │   ├── News.vue
│   │   │   ├── Register.vue
│   │   │   ├── RoomBooking.vue
│   │   │   └── RoomEnvironment.vue
│   │   ├── router/
│   │   │   └── index.ts
│   │   ├── stores/
│   │   │   ├── admin.ts
│   │   │   ├── booking.ts
│   │   │   └── user.ts
│   │   ├── styles/
│   │   │   ├── global.scss
│   │   │   ├── mixins.scss
│   │   │   └── variables.scss
│   │   ├── types/
│   │   │   └── index.ts
│   │   ├── utils/
│   │   │   ├── date.ts
│   │   │   └── validation.ts
│   │   ├── App.vue
│   │   └── main.ts
│   ├── index.html
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
├── backend/
│   ├── src/
│   │   ├── config/
│   │   │   ├── database.js
│   │   │   └── jwt.js
│   │   ├── controllers/
│   │   │   ├── authController.js
│   │   │   ├── bookingController.js
│   │   │   ├── dashboardController.js
│   │   │   └── userController.js
│   │   ├── middleware/
│   │   │   ├── auth.js
│   │   │   ├── errorHandler.js
│   │   │   └── validation.js
│   │   ├── models/
│   │   │   ├── Admin.js
│   │   │   ├── Booking.js
│   │   │   ├── Restaurant.js
│   │   │   ├── RoomType.js
│   │   │   └── User.js
│   │   ├── routes/
│   │   │   ├── auth.js
│   │   │   ├── bookings.js
│   │   │   ├── dashboard.js
│   │   │   └── users.js
│   │   ├── services/
│   │   │   ├── authService.js
│   │   │   ├── bookingService.js
│   │   │   └── userService.js
│   │   ├── utils/
│   │   │   ├── jwt.js
│   │   │   ├── password.js
│   │   │   └── response.js
│   │   ├── app.js
│   │   └── server.js
│   ├── .env
│   └── package.json
├── database/
│   ├── init.sql
│   ├── schema.sql
│   └── seed.sql
└── README.md
```

---

## 七、执行计划说明

### 执行顺序（按阶段）

| 阶段 | 内容 | 涉及文件 |
|------|------|---------|
| **Phase 1** | 后端架构搭建 | backend/ 目录下所有文件 |
| **Phase 2** | 前端状态管理与 API 对接 | stores/、api/ 目录下文件 |
| **Phase 3** | UI 组件库整合 | 现有组件和页面改造 |
| **Phase 4** | TypeScript 迁移 | 所有 .js 文件改为 .ts |
| **Phase 5** | 样式优化与图标库 | styles/ 目录下文件 |
| **Phase 6** | 测试与优化 | 全项目测试 |

### TypeScript 迁移策略

1. **逐步迁移**：每个文件单独迁移，迁移后立即测试
2. **保留兼容**：迁移过程中保留 .js 文件作为 fallback
3. **类型定义**：先创建 types/index.ts，再逐步为每个文件添加类型

### 注意事项

1. **现有文件保留**：迁移过程中不删除现有文件，先复制后修改
2. **测试优先**：每个阶段完成后运行构建测试，确保项目可运行
3. **配置同步**：前端代理配置需与后端 API 路径保持一致

---

## 八、API接口规范

### 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 接口清单

#### 用户认证接口
| 接口 | 方法 | 路径 |
|------|------|------|
| 用户登录 | POST | `/api/auth/login` |
| 用户注册 | POST | `/api/auth/register` |
| 管理员登录 | POST | `/api/auth/admin-login` |
| 获取当前用户 | GET | `/api/auth/me` |

#### 预订管理接口
| 接口 | 方法 | 路径 |
|------|------|------|
| 获取预订列表 | GET | `/api/bookings` |
| 获取预订详情 | GET | `/api/bookings/:id` |
| 创建预订 | POST | `/api/bookings` |
| 更新预订状态 | PUT | `/api/bookings/:id/status` |
| 删除预订 | DELETE | `/api/bookings/:id` |

#### 用户管理接口
| 接口 | 方法 | 路径 |
|------|------|------|
| 获取用户列表 | GET | `/api/users` |
| 获取用户详情 | GET | `/api/users/:id` |
| 更新用户信息 | PUT | `/api/users/:id` |
| 删除用户 | DELETE | `/api/users/:id` |

#### 数据统计接口
| 接口 | 方法 | 路径 |
|------|------|------|
| 获取统计数据 | GET | `/api/dashboard/stats` |

---

## 九、技术栈说明

### 后端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | 18+ | 运行环境 |
| Express | 4.x | 后端框架 |
| Sequelize | 6.x | ORM 框架 |
| MySQL | 8.x | 数据库 |
| JWT | jsonwebtoken | 身份认证 |
| bcrypt | 5.x | 密码加密 |

### 前端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.x | 前端框架 |
| Vue Router | 4.x | 路由管理 |
| Pinia | 2.x | 状态管理 |
| Element Plus | 2.x | UI 组件库 |
| Axios | 1.x | HTTP 请求 |
| TypeScript | 5.x | 类型系统 |
| SCSS | latest | CSS 预处理器 |
| Lucide Icons | latest | 图标库 |
| Vite | 5.x | 构建工具 |

---

## 十、验收标准

### 前端验收标准
- ✅ Vue 3 + Composition API 正确使用
- ✅ Pinia 状态管理配置完成，状态持久化正常
- ✅ Axios 请求封装完成，拦截器正常工作
- ✅ Element Plus UI 组件整合完成
- ✅ TypeScript 迁移完成，无类型错误
- ✅ SCSS 样式预处理器配置完成
- ✅ Lucide Icons 图标库整合完成
- ✅ 响应式布局在不同设备上正常显示

### 后端验收标准
- ✅ Express 项目搭建完成，能正常启动
- ✅ MySQL 数据库连接成功，表结构创建完成
- ✅ JWT 身份认证机制正常工作
- ✅ 所有 API 接口返回统一格式
- ✅ 用户 CRUD 功能正常
- ✅ 预订 CRUD 功能正常
- ✅ 数据统计接口正常
- ✅ CORS 跨域处理配置完成

### 整体验收标准
- ✅ 前后端分离架构搭建完成
- ✅ 前端与后端通过 API 接口正常通信
- ✅ 所有功能模块测试通过
- ✅ 项目能正常打包和部署
- ✅ 代码结构清晰，符合规范
- ✅ 项目文档完整

---

*本方案创建于 2026-07-12*
*适用于「基于 Vue 3 的星级酒店预订与管理系统的设计与实现」项目*